.github/
.git/
packwiz
*.zip
*.mrpack
scripts/

# Local / dev-only — same idea as .gitignore: never ship these to clients via the packwiz index
.agent/
.cursor/
logs/

# Dev documentation in repo; not required to run the game (avoids extra downloads on install)
docs/

# Repo readme; GitHub Pages often does not mirror this path like raw git — excluding avoids installer 404s
README.md
CHANGELOG.md
instance.cfg
instance.dev.cfg
mmc-pack.json
packwiz-installer-bootstrap.jar

# Client-local Sodium fingerprint (regenerated per machine; do not ship)
config/sodium-fingerprint.json

# Spark profiler temp folders (runtime noise)
config/spark/tmp/
config/spark/tmp-client/
